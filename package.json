{
  "name": "push-metrics",
  "version": "0.0.1",
  "description": "A github action for pushing github actions metrics",
  "main": "push-metrics.js",
  "scripts": {
    "prepare": "esbuild src/index.ts --bundle --platform=node --target=node16 --tsconfig=./tsconfig.build.json --outfile=dist/index.js",
    "format": "npx prettier --write .",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.8.2",
    "@actions/github": "^5.0.3",
    "type-fest": "^2.13.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.3.0",
    "@octokit/webhooks-types": "^5.6.0",
    "@types/jest": "^28.1.1",
    "@types/node": "^17.0.41",
    "esbuild": "^0.14.43",
    "jest": "^28.1.1",
    "jest-mock-extended": "^2.0.6",
    "nock": "^13.2.6",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.3"
  }
}
